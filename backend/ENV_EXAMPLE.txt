PORT=5000
NODE_ENV=development
JWT_SECRET=votre_secret_jwt_tres_securise_changez_moi_123456789
ADMIN_PASSWORD=votre_mot_de_passe_admin

# Option 1 : Connection string complète (recommandé pour Neon)
# DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Option 2 : Variables séparées (pour PostgreSQL local ou autres services)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=correcteur_db
DB_USER=postgres
DB_PASSWORD=votre_mot_de_passe

